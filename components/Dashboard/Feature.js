import React from "react";

export default function Feature() {
  return (
    <div
      className="rounded-xl bg-cover   p-10 shadow-xl"
      style={{
        minHeight: "300px",
        display: "flex",
        flexDirection: "column",
        justifyContent: "flex-end",
      }}
    >
      <div></div>
    </div>
  );
}
